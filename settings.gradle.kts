pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "Type_Hero"
include(":app")
include(":presentation")
include(":domain")
include(":data")
